mutation modifyPassword($confirmPassword: String!, $password: String!) {
    modifyPassword(confirmPassword: $confirmPassword, password: $password) {
        status
        errors {
            field
            message
        }
    }
}

mutation sendFormSupport($fullName: String!, $email: String!, $subject: String!, $message: String!) {
    sendFormSupport(fullName: $fullName, email: $email, subject: $subject, message: $message) {
        status
        errors {
            field
            message
        }
    }
}